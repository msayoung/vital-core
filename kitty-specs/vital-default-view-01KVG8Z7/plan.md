# Implementation Plan: VITAL default view - tighter priority filter

**Status**: RECONSTRUCTED FROM SHIPPED WORK (#146)
**Date reconstructed**: 2026-06-20
**Spec**: [spec.md](spec.md)

## Summary

Show only high-value VITAL accessibility errors by default while preserving a Show everything toggle for the full finding set.

## Technical Context

**Language/Version**: Node.js ESM >=20
**Primary Dependencies**: Existing report-html and accessibility priority helpers; no new dependencies
**Storage**: Display-only change; data records unchanged
**Testing**: Node built-in test runner via npm run test:unit
**Target Platform**: Static report HTML generated by aggregate.js
**Constraints**: All default visibility logic must be centralized through priorityTier()

## Charter Check

- Preserves plain Node.js ESM with no build step.
- Preserves historical weekly data compatibility unless explicitly noted otherwise.
- Keeps report core content usable as generated static HTML.
- Uses focused unit coverage for behavior changes.
- Does not introduce secrets or external service requirements.

## Work Packages

### WP01 - Priority tier contract and unit test

Define the VITAL default visibility contract in priorityTier() and prove it with the canonical fixture.

**Requirement refs**: FR-01, FR-02, C-01, NFR-01
**Owned files**: src/lib/accessibility-priority.js, tests/unit/**/*.test.js
**Validation**: Run npm run test:unit.

### WP02 - Report default filter and toggle

Apply the priority tier contract in the accessibility report and keep full findings available on demand.

**Requirement refs**: FR-03, FR-04
**Owned files**: src/report-html.js
**Validation**: Generate or inspect report HTML and run npm run test:unit.

## Validation Plan

- Run `npm run test:unit` for all specs.
- Run `npm run test:e2e` when changing scan, aggregate, generated report, or weekly pipeline behavior.
- Run `git diff --check` before commit.

## Rollback Plan

Revert the focused implementation commit for this spec. These shipped-spec reconstruction artifacts do not require product rollback; they document the already-shipped implementation boundary.

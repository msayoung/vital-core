# Tasks Directory

This directory contains work package (WP) prompt files.

## Work packages

- WP01 — Correctness fixes in `src/lib/wcag.js` and `src/lib/acr.js`
- WP02 — Wire into aggregate pipeline + accessibility page download link

All WP files are stored flat in `tasks/`. Status is tracked in `status.events.jsonl`.
